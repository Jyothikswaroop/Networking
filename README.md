# Networking Configurations CCNA(Routers & Switches)
# Switch configured.
# Router configured.
# StaticIP configured.
# DHCP(Dynamic Host Configuration Protocol) and DNS(Domain Name Service) configured.
ACL(Access Control List) both Standard and Extended Configured.
VLAN configured.
